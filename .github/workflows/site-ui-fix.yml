name: Aplicar interfaz WR

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "La aplicación automática de interfaz está desactivada para preservar la versión restaurada."
